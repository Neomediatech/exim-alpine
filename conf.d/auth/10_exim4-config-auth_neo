dovecot_plain:
  driver = dovecot
  public_name = PLAIN
  server_socket = /data/common/dovecot-auth
  server_set_id = $auth1

dovecot_login:
  driver = dovecot
  public_name = LOGIN
  server_socket = /data/common/dovecot-auth
  server_set_id = $auth1
